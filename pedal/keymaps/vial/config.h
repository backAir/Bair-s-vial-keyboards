#pragma once

/* Vial UID (must be unique, exactly 8 bytes) */
#define VIAL_KEYBOARD_UID {0x7C, 0x92, 0x7C, 0x92, 0x00, 0x01, 0x00, 0x01}

/* Unlock combo (example: top-left + top-right keys) */
#define VIAL_UNLOCK_COMBO_ROWS {0, 0}
#define VIAL_UNLOCK_COMBO_COLS {0, 0}
